# Visual validation — registration and lighthouse update

Desktop `/register` shows the enlarged header mark and the new country-code selector area begins cleanly in the information step. Desktop `/home` shows the lighthouse tower and beam visible on the right side of the fixed hero without obscuring the mission console. The cinematic root remains readable during the opening scene.

At 375px, `/register` preserves the inset dossier layout and stacked navigation. At 375px, `/home` remains a single-screen composition, but the lighthouse signal/tower currently sits above the hero copy and crosses the title/action area; the next refinement moves it behind the copy layer and farther right with lower opacity. The cinematic root remains within the viewport during the opening scene.
